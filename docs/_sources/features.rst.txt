Features
=================

 + supports count/TPM data as input.
 + supports outputing cell pseudotimes and gene ranks for GSEA analysis.
 + supports converstion from Ensembl to HUGO, so the proposed genes are more readable.